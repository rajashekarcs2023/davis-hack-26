"""Synthetic and reference inputs for the AgriScout data pipeline.

The `synthetic_field.py` script generates a deterministic 256x256 4-band
scene with a clear row-aligned anomaly in zone B3, suitable for unit
tests and for running the pipeline end-to-end without needing a real
Sentinel-2 download.
"""
