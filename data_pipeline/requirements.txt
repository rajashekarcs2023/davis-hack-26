# Core deps — the pipeline runs end-to-end on these three. Same versions
# pinned by the backend pyproject.toml so a single venv satisfies both.
numpy>=2.1.0
Pillow>=10.4.0
httpx>=0.27.0

# Test runner — only needed for `pytest data_pipeline/tests`.
pytest>=8.3.0
pytest-asyncio>=0.24.0

# Optional — required ONLY if you want to ingest raw Sentinel-2 GeoTIFFs
# (.tif / .jp2) directly out of a Copernicus Open Hub download. Skip this
# install and the pipeline still works on 4-band PNG/JPG and .npy inputs.
# rasterio>=1.3.0
